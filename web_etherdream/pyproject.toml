[project]
name = "web-etherdream"
version = "0.1.0"
description = "Interactive web interface for Ether Dream laser control"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0.0",
    "flask-cors>=4.0.0",
]
